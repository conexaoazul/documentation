name: Blue Docs Immutable Image

on:
  pull_request:
    branches: ["blueconnect"]
  push:
    branches: ["blueconnect"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  actions: write

concurrency:
  group: blue-docs-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-image:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Validate Blue Connect navigation contract
        run: python3 scripts/validate_blueconnect_navigation.py

      - name: Validate Blue Connect acquisition contract
        run: python3 scripts/validate_blueconnect_acquisition.py

      - name: Build validation image
        run: |
          set -Eeuo pipefail
          docker build \
            --file Dockerfile.blue-docs \
            --build-arg "SOURCE_SHA=${GITHUB_SHA}" \
            --tag blue-docs:validation \
            .

      - name: Validate health, index and attribution
        run: |
          set -Eeuo pipefail
          docker run -d --rm --name blue-docs-validation -p 127.0.0.1:18080:8080 blue-docs:validation
          trap 'docker stop blue-docs-validation >/dev/null 2>&1 || true' EXIT
          for attempt in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:18080/healthz | grep -Fq ok; then
              break
            fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:18080/index.html > /tmp/index.html
          grep -Eiq '<html|<!doctype' /tmp/index.html
          curl -fsS http://127.0.0.1:18080/BLUE_SOURCE.txt > /tmp/blue-source.txt
          grep -Fq 'License: CC-BY-SA-4.0' /tmp/blue-source.txt
          grep -Fq "Commit: ${GITHUB_SHA}" /tmp/blue-source.txt

  publish-image:
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/blueconnect'
    needs: validate-image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set canonical release tags
        id: tags
        run: |
          set -Eeuo pipefail
          echo "sha_tag=ghcr.io/conexaoazul/odoo-documentation:blueconnect-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          echo "float_tag=ghcr.io/conexaoazul/odoo-documentation:blueconnect" >> "$GITHUB_OUTPUT"

      - name: Build and publish immutable image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.blue-docs
          push: true
          build-args: |
            SOURCE_SHA=${{ github.sha }}
          tags: |
            ${{ steps.tags.outputs.sha_tag }}
            ${{ steps.tags.outputs.float_tag }}

      - name: Trigger automatic deployment to dev1
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -Eeuo pipefail
          gh workflow run deploy-docs-erp-dev1.yml \
            --repo conexaoazul/documentation \
            --ref blueconnect \
            -f image="ghcr.io/conexaoazul/odoo-documentation:blueconnect-${GITHUB_SHA}"
